# 快速開始指南

## ✨ 系統已就緒！

你的 LeetCode 150 練習系統已經完成設置，包含：

- ✅ 23 個主題分類（已預先建立）
- ✅ CLI 工具（可使用 npm 指令）
- ✅ 範例題目：Two Sum（array-string 主題下）

## 🚀 立即開始

### 1. 查看所有主題和題目

```bash
npm run list
```

### 2. 新增題目

選擇一個主題並新增題目（互動式介面）：

```bash
npm run problem array-string
```

系統會詢問：

- Problem title（例如：Valid Parentheses）
- Problem number（例如：20）
- Difficulty（Easy/Medium/Hard）
- LeetCode URL（會自動產生，可修改）

### 3. 查看產生的檔案

當你新增一個題目後，會產生：

```
problems/{topic-slug}/{problem-slug}/
├── solution.ts    # 解答程式（含測試案例）
└── notes.md       # 學習筆記（含模板）
```

### 4. 撰寫解答

編輯 `solution.ts`，實作你的解法。

### 5. 測試解答

```bash
npm run run {problem-slug}
```

例如：

```bash
npm run run two-sum
```

### 6. 記錄學習筆記

編輯 `notes.md`，填寫：

- 底層邏輯：用一段話總結核心觀念
- 關鍵重點：列出需要注意的技巧
- 複雜度分析
- 學習筆記：心得、錯誤記錄、優化思路

## 📝 常用指令

| 指令                      | 說明               | 範例                           |
| ------------------------- | ------------------ | ------------------------------ |
| `npm run list`            | 列出所有主題和題目 | -                              |
| `npm run topic <name>`    | 建立新主題         | `npm run topic "Greedy"`       |
| `npm run problem <topic>` | 新增題目到主題     | `npm run problem array-string` |
| `npm run run <slug>`      | 執行題目解答       | `npm run run two-sum`          |
| `npm run cli -- --help`   | 查看完整指令說明   | -                              |

## 🎯 學習工作流程範例

1. **選擇今天要練習的主題**

   ```bash
   npm run list
   ```

2. **新增一個題目**

   ```bash
   npm run problem two-pointers
   # 輸入題目資訊...
   ```

3. **撰寫解答**
   - 開啟 VSCode
   - 編輯產生的 `solution.ts`

4. **測試並調整**

   ```bash
   npm run run your-problem-slug
   ```

5. **記錄心得**
   - 編輯 `notes.md`
   - 整理解題邏輯和重點

6. **複習時更新筆記**
   - 在 notes.md 的「後續複習」區塊記錄新想法

## 📂 專案結構

```
leetcode/
├── cli/                      # CLI 工具
├── config/
│   └── topics.json          # 所有題目的 metadata
├── problems/                # 你的解答區
│   ├── array-string/
│   │   └── two-sum/        # 範例題目
│   │       ├── solution.ts
│   │       └── notes.md
│   ├── two-pointers/
│   ├── sliding-window/
│   └── ...（其他 23 個主題）
├── templates/               # 檔案模板
├── package.json
└── README.md               # 完整文件
```

## 💡 提示

- 每個題目的 `solution.ts` 都可以直接執行
- 測試案例寫在 solution.ts 的 `runTests()` 函式中
- notes.md 使用模板，逐步填寫即可
- 所有檔案使用 slug 命名（小寫、破折號分隔）
- topics.json 會自動更新，無需手動編輯

## 🎓 範例：Two Sum

查看 `problems/array-string/two-sum/` 來了解：

- 如何撰寫解答
- 如何寫測試案例
- 如何記錄學習筆記

執行看看：

```bash
npm run run two-sum
```

---

**現在開始你的 LeetCode 150 練習之旅吧！** 🚀

有問題？查看 [README.md](README.md) 獲取完整文件。
